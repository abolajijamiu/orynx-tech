services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: orynx
      POSTGRES_PASSWORD: orynx
      POSTGRES_DB: orynx
    ports: ["5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U orynx"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
